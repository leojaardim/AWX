---
- hosts: all
  gather_facts: true
  become: true
  tasks:
    - yum:
        name: vim
      when: ansible_os_family == "RedHat"
    - apt:
        name: cups
      when: ansible_os_family == "Debian"
