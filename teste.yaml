---
- hosts: all
  gather_facts: true
  become: true
  tasks:
    - yum:
        name: vim
      when: ansible_os_family == "RedHat"
      args:
        warn: false
    - apt:
        name: vim
      when: ansible_os_family == "Debian"
      args:
        warn: false
